package org.example.structural.flyweight.helpers;

// Estrutura composta (Composite Pattern junto com Flyweight).

public class Window {
    public void drawText(String text) {
        System.out.println("Renderizando: " + text);
    }
}
